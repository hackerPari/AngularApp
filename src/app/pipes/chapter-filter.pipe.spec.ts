import { ChapterFilterPipe } from './chapter-filter.pipe';

describe('ChapterFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ChapterFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
